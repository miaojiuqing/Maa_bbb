# a = 13
# b = 34
# a = a + b
# b = a - b
# a = a - b
# print(a, b)
# range(10)

# a = 2
# a**= 3//2的三次方
# print(a)

# a = 0# 初始化变量a为1（阶乘的初始值应为1）
# for i in (1, 2, 3, 4, 5):# 使用for循环遍历数字1到5
#     a *= i # 累乘计算阶乘
# print(a)# 输出5的阶乘结果：1×2×3×4×5 = 120

def a(b):           # 定义函数a，参数为b
    try:            # 尝试执行下面的操作
        return b*5  # 返回b乘以5的结果
    except:         # 如果上面操作出错（比如b不是数字）
        return b    # 就返回b本身

print(a("1"))       # 调用函数a，传入字符串"1"，输出结果